---
alwaysApply: true
---

# 🗂 Context7 — полный контекст проекта

## Проект: Baumaster Frankfurt

**Тип:** Корпоративный сайт строительной фирмы + Админ-панель  
**Технологии:** PHP 7.4+, TailwindCSS, Vanilla JS, SQLite/JSON  
**Статус:** Полностью реализован (100%)  
**Дата обновления:** 2025-01-27  
**Анализ:** Завершен согласно roadmap.md

## Файлы и модули

### Основные конфигурационные файлы

- `config.php` - основные настройки проекта, константы, пути
- `functions.php` - общие PHP функции (безопасность, валидация, форматирование)
- `database.php` - система работы с БД (SQLite + JSON fallback)

### Frontend (Публичная часть)

- `index.php` - главная страница с hero, услуги, портфолио, отзывы
- `about.php` - страница о компании
- `services.php` - страница услуг
- `portfolio.php` - страница портфолио
- `review.php` - страница отзывов
- `contact.php` - страница контактов
- `blog.php` - страница блога/FAQ

### Admin Panel (Админ-панель)

- `admin/index.php` - Dashboard с статистикой и быстрыми действиями
- `admin/login.php` - авторизация
- `admin/services.php` - управление услугами
- `admin/portfolio.php` - управление портфолио
- `admin/reviews.php` - управление отзывами
- `admin/blog.php` - управление блогом/статьями
- `admin/users.php` - управление пользователями
- `admin/settings.php` - настройки сайта
- `admin/stats.php` - статистика и аналитика

### Компоненты и UI

- `components/admin_layout.php` - основной layout админки
- `components/auth_layout.php` - layout для авторизации
- `ui/base.php` - базовые UI компоненты
- `ux/layout.php` - layout для frontend
- `ux/components.php` - компоненты для frontend
- `ux/data.php` - функции получения данных

### Интеграции

- `integrations/analytics.php` - Google Analytics
- `integrations/email.php` - email уведомления
- `integrations/maps.php` - Google Maps
- `integrations/i18n.php` - многоязычность

### SEO и оптимизация

- `seo/sitemap.php` - генератор sitemap
- `seo/robots.txt` - настройки для поисковиков
- `seo/seo_utils.php` - SEO утилиты
- `seo/performance.php` - оптимизация производительности
- `seo/image_optimizer.php` - оптимизация изображений

### Данные и хранение

- `data/` - JSON файлы для хранения данных
  - `users.json` - пользователи
  - `services.json` - услуги
  - `portfolio.json` - портфолио
  - `reviews.json` - отзывы
  - `blog_posts.json` - статьи блога
  - `settings.json` - настройки
  - `activity_log.json` - логи активности

### Многоязычность

- `lang/ru.json` - русский язык
- `lang/de.json` - немецкий язык
- `lang/en.json` - английский язык

## Функции

### Основные CRUD функции

- `createAd(data)` - создание объявления/услуги
- `getAds(filter)` - получение списка объявлений/услуг
- `deleteAd(id)` - удаление объявления/услуги
- `validateForm(formData)` - валидация форм

### Функции безопасности

- `sanitize_input($data)` - санитизация данных
- `validate_email($email)` - валидация email
- `generate_csrf_token()` - генерация CSRF токена
- `verify_csrf_token($token)` - проверка CSRF токена
- `hash_password($password)` - хеширование пароля
- `verify_password($password, $hash)` - проверка пароля

### Функции авторизации

- `is_logged_in()` - проверка авторизации
- `get_current_admin_user()` - получение данных пользователя
- `require_role($role)` - проверка роли
- `has_permission($action, $user)` - проверка прав доступа
- `login_user($user_data)` - авторизация пользователя
- `logout_user()` - выход из системы

### Функции работы с файлами

- `upload_file($file, $destination, $allowed_types)` - загрузка файлов
- `delete_file($filepath)` - удаление файлов
- `read_json_file($filepath)` - чтение JSON файлов
- `write_json_file($filepath, $data)` - запись JSON файлов

### Функции форматирования

- `format_date($date, $format)` - форматирование даты
- `format_filesize($bytes, $precision)` - форматирование размера файла
- `truncate_text($text, $length, $suffix)` - обрезка текста
- `format_price($price, $currency)` - форматирование цены

### Функции языка и локализации

- `get_current_language()` - получение текущего языка
- `set_language($lang)` - установка языка
- `load_language($lang)` - загрузка языковых данных
- `__($key, $fallback)` - получение переведенного текста

### Функции настроек

- `get_setting($key, $default)` - получение настройки
- `set_setting($key, $value, $category, $description)` - установка настройки
- `get_settings_by_category($category)` - получение настроек по категории

### Функции пагинации

- `create_pagination($total_items, $current_page, $per_page)` - создание пагинации

### Функции отладки и логирования

- `write_log($message, $level)` - запись в лог
- `debug_dump($var, $die)` - дамп переменной для отладки
- `json_response($data, $status_code)` - JSON ответ

## Переменные

### Константы конфигурации

- `SITE_NAME` - название сайта
- `SITE_URL` - URL сайта
- `ADMIN_URL` - URL админки
- `VERSION` - версия проекта
- `DEBUG_MODE` - режим отладки
- `DEFAULT_LANG` - язык по умолчанию

### Пути к папкам

- `ADMIN_PATH` - путь к админке
- `COMPONENTS_PATH` - путь к компонентам
- `UI_PATH` - путь к UI элементам
- `ASSETS_PATH` - путь к ресурсам
- `LANG_PATH` - путь к языковым файлам
- `DATA_PATH` - путь к данным
- `UPLOADS_PATH` - путь к загруженным файлам

### Настройки базы данных

- `DB_TYPE` - тип БД (sqlite/json)
- `DB_PATH` - путь к SQLite файлу
- `DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS` - настройки MySQL

### Настройки безопасности

- `HASH_ALGO` - алгоритм хеширования
- `CSRF_TOKEN_NAME` - имя CSRF токена
- `MAX_LOGIN_ATTEMPTS` - максимальное количество попыток входа
- `LOGIN_LOCKOUT_TIME` - время блокировки

### Настройки загрузки файлов

- `MAX_FILE_SIZE` - максимальный размер файла
- `ALLOWED_IMAGE_TYPES` - разрешенные типы изображений
- `ALLOWED_DOC_TYPES` - разрешенные типы документов

### Email настройки

- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD` - настройки SMTP
- `FROM_EMAIL`, `FROM_NAME` - отправитель

### SEO настройки

- `DEFAULT_META_TITLE` - заголовок по умолчанию
- `DEFAULT_META_DESCRIPTION` - описание по умолчанию
- `DEFAULT_KEYWORDS` - ключевые слова по умолчанию

## Связи

### Frontend связи

- `index.php` → `ux/layout.php` → `ux/components.php` → `ux/data.php`
- `services.php` → `ux/data.php` → `database.php`
- `portfolio.php` → `ux/data.php` → `database.php`
- `review.php` → `ux/data.php` → `database.php`
- `blog.php` → `ux/data.php` → `database.php`

### Admin связи

- `admin/index.php` → `components/admin_layout.php` → `database.php`
- `admin/services.php` → `components/admin_layout.php` → `database.php`
- `admin/portfolio.php` → `components/admin_layout.php` → `database.php`
- `admin/reviews.php` → `components/admin_layout.php` → `database.php`
- `admin/blog.php` → `components/admin_layout.php` → `database.php`
- `admin/users.php` → `components/admin_layout.php` → `database.php`
- `admin/settings.php` → `components/admin_layout.php` → `database.php`

### Общие связи

- `config.php` → `functions.php` → `database.php`
- `functions.php` → `database.php` → JSON файлы в `data/`
- `integrations/` → `config.php` → внешние сервисы
- `seo/` → `config.php` → SEO оптимизация

### База данных

- SQLite: `data/baumaster.db` (основная БД)
- JSON fallback: файлы в `data/` (резервное хранение)
- Таблицы: users, services, portfolio, reviews, blog_posts, settings, activity_log

## Статус реализации

### ✅ Завершено (100%)

- Структура проекта и конфигурация
- Система авторизации и безопасности
- UI компоненты и layouts
- Dashboard админки
- CRUD операции для всех сущностей
- Управление услугами, портфолио, отзывами, блогом
- Управление пользователями
- Настройки сайта
- Статистика и аналитика
- SEO оптимизация
- Интеграции (Analytics, Maps, Email)
- Многоязычность
- Тестирование и документация

### 📊 Статистика проекта

- **Файлов PHP:** 50+
- **Строк кода:** 15,000+
- **Функций:** 100+
- **Таблиц БД:** 7
- **Языков:** 3 (ru, de, en)
- **Компонентов UI:** 20+

### 🎯 Ключевые особенности

- Полная админ-панель с CRUD операциями
- Система ролей и прав доступа
- Многоязычная поддержка
- SEO оптимизация
- Адаптивный дизайн
- Безопасность (CSRF, валидация, хеширование)
- Логирование активности
- Экспорт данных
- Интеграции с внешними сервисами
