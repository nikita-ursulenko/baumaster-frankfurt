---
alwaysApply: true
---

# 🔧 Правила AICursor

## 1️⃣ Основной порядок

1. Сначала читать roadmap.md
2. Затем `.cursor/rules/context7.mdc` → полный контекст проекта
3. Далее `.cursor/rules/memory.mdc` и `.cursor/rules/snapshot.mdc` → краткий контекст и прогресс
4. Загружать `.cursor/rules/playwright.mdc` для статуса тестов
5. Определять язык/стек проекта (React, PHP, Node, Python и др.)

## 2️⃣ Разбор задач

- Проверять существование файлов и функций
- Разбивать задачи на подзадачи
- Прогресс фиксируется только после успешного теста

## 3️⃣ Работа с .mdc файлами

**Все .mdc файлы находятся в `.cursor/rules/`:**

- `.cursor/rules/snapshot.mdc` → обновляется после каждого шага с изменениями
- `.cursor/rules/context7.mdc` → обновляется при добавлении/изменении файлов и связей
- `.cursor/rules/memory.mdc` → лог всех действий
- `.cursor/rules/playwright.mdc` → результаты тестов
- `.cursor/rules/version.mdc` → фиксирует версию после завершения подзадачи

## 4️⃣ Тестирование

- Запуск Playwright или подходящего фреймворка для стека
- Результаты фиксировать в `.cursor/rules/playwright.mdc` и `.cursor/rules/memory.mdc`
- Шаг не фиксируется, если тест не пройден

## 5️⃣ Git

- Commit после каждого шага, включая все .mdc файлы из `.cursor/rules/`
- Сообщение: `[Cursor vX.X.X] Завершена подзадача: <название>`
- Push в конце сессии

## 6️⃣ Универсальность

- AICursor сам определяет стек и язык
- Snapshot и context7.mdc универсальны для любого проекта
- Все .mdc файлы всегда в `.cursor/rules/`

## 7️⃣ Автокоррекция

- Cursor исправляет устаревшие .mdc файлы автоматически
- Никогда не придумывает функции или переменные
- Всегда проверять правильность путей к .mdc файлам

## 8️⃣ Структура .mdc файлов

```
.cursor/rules/
├── context7.mdc    # Полный контекст проекта
├── memory.mdc      # Лог действий и прогресс
├── snapshot.mdc    # Краткий контекст
├── playwright.mdc  # Статус тестирования
├── version.mdc     # Контроль версий
└── rules.mdc       # Эти правила (этот файл)
```
